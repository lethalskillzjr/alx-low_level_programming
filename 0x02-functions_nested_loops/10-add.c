#include "main.h"
#include <stdio.h>


/**
 * add - check the code
 * @a: the integer value it receives
 * @b: the integer value it receives
 * Return: Always 0.
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
