#include "main.h"
#include <stdio.h>

/**
 * _abs - Entry point
 * Description: prints the absolute value of a number
 * @n: n is an integer
 * Return: 0
 */
int _abs(int n)
{

if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
